generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model UserPublic {
  id         String   @id @default(uuid())
  first_name String
  last_name  String
  email      String
  password   String
  createdAt  DateTime @default(now())
}
